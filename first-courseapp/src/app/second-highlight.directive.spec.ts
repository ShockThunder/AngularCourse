import { SecondHighlightDirective } from './second-highlight.directive';

describe('SecondHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
